import React from 'react';
import ReactDOM from 'react-domclient';
import App from '.App';
import '.style.css';  możesz dodać inne style

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  React.StrictMode
    App 
  React.StrictMode
);
